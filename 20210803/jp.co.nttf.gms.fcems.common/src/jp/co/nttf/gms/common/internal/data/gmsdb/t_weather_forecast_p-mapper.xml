<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
  "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.nttf.gms.common.internal.data.gmsdb.TWeatherForecastPMapper">
  <!-- テーブル名 -->
  <sql id="tableName">t_weather_forecast_p</sql>
  <!-- 全カラム名 -->
  <sql id="allColumns">
    distribution_time, location_id, pressure_level, forecast_time, geopotential_height, u_wind, v_wind, temperature, relative_humidity, updraft
  </sql>
  <!-- 挿入クエリ -->
  <insert id="insert" parameterType="TWeatherForecastP">
    INSERT INTO <include refid="tableName"/>
    (distribution_time, location_id, pressure_level, forecast_time, geopotential_height, u_wind, v_wind, temperature, relative_humidity, updraft)
    values (
      #{distribution_time}, #{location_id}, #{pressure_level}, #{forecast_time}, #{geopotential_height}, #{u_wind}, #{v_wind}, #{temperature}, #{relative_humidity}, #{updraft}
    )
  </insert>
  <!-- 全件取得クエリ -->
  <select id="selectAll" resultType="TWeatherForecastP">
    SELECT <include refid="allColumns"/>
    FROM <include refid="tableName"/>
  </select>
  <!-- 特定の配信における登録件数取得クエリ -->
  <select id="countDataInDistribution" resultType="Integer">
    SELECT COUNT(*)
    FROM <include refid="tableName"/>
    WHERE distribution_time = #{distributionTime}
  </select>
</mapper>
