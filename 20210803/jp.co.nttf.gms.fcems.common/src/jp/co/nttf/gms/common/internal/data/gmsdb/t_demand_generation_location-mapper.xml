<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
  "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.nttf.gms.common.internal.data.gmsdb.TDemandGenerationLocationMapper">
  <!-- テーブル名 -->
  <sql id="tableName">t_demand_generation_location</sql>
  <!-- 全カラム名 -->
  <sql id="allColumns">
    d_g_location_id, g_location_id, p_location_id, demand_measurepoint_list_id, generation_measurepoint_list_id, gmu_id, soc_mod_setting
  </sql>
  <!-- 全件取得クエリ -->
  <select id="selectAll" resultType="TDemandGenerationLocation">
    SELECT <include refid="allColumns"/>
    FROM <include refid="tableName"/>
  </select>
  <!-- 特定のIDのデータを取得するクエリ -->
  <select id="selectById" resultType="TDemandGenerationLocation">
    SELECT <include refid="allColumns"/>
    FROM <include refid="tableName"/>
    WHERE
      d_g_location_id = #{d_g_location_id}
  </select>
</mapper>
