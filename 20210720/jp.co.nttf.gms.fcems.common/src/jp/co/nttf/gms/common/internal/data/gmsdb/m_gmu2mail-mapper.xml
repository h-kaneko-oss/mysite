<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
  "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.nttf.gms.common.internal.data.gmsdb.MGmu2mailMapper">
  <!-- テーブル名 -->
  <sql id="tableName">m_gmu2mail</sql>
  <!-- 全カラム名 -->
  <sql id="allColumns">
    userid, no, name, mailaddress, cemsdrsend, settingsend, testsend, bulksend, pointsend, reenergysend, fwsend, refusalsend, note
  </sql>
  <!-- メンテナンスデバイス設定を登録する -->
  <select id="getGmu2mailList" resultType="MGmu2mail" parameterType="MGmu2mail">
    SELECT <include refid="allColumns"/>
    FROM <include refid="tableName"/>
    WHERE userid = #{userid}
    <if test="cemsdrsend != null">AND cemsdrsend = #{cemsdrsend}</if>
    <if test="settingsend != null">AND settingsend = #{settingsend}</if>
    <if test="testsend != null">AND testsend = #{testsend}</if>
    <if test="bulksend != null">AND bulksend = #{bulksend}</if>
    <if test="pointsend != null">AND pointsend = #{pointsend}</if>
    <if test="reenergysend != null">AND reenergysend = #{reenergysend}</if>
    <if test="fwsend != null">AND fwsend = #{fwsend}</if>
    <if test="refusalsend != null">AND refusalsend = #{refusalsend}</if>
    ORDER BY no
  </select>
</mapper>
