<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
  "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.nttf.gms.common.internal.data.gmsdb.TWeatherForecastGMapper">
  <!-- テーブル名 -->
  <sql id="tableName">t_weather_forecast_g</sql>
  <!-- 全カラム名 -->
  <sql id="allColumns">
    distribution_time, location_id, forecast_time, surface_pressure, u_wind, v_wind, temperature, relative_humidity, precipitation, total_cloud_cover, high_cloud_cover, medium_cloud_cover, low_cloud_cover, sunshine
  </sql>
  <!-- 挿入クエリ -->
  <insert id="insert" parameterType="TWeatherForecastG">
    INSERT INTO <include refid="tableName"/>
    (distribution_time, location_id, forecast_time, surface_pressure, u_wind, v_wind, temperature, relative_humidity, precipitation, total_cloud_cover, high_cloud_cover, medium_cloud_cover, low_cloud_cover, sunshine)
    values (
      #{distribution_time}, #{location_id}, #{forecast_time}, #{surface_pressure}, #{u_wind}, #{v_wind}, #{temperature}, #{relative_humidity}, #{precipitation}, #{total_cloud_cover}, #{high_cloud_cover}, #{medium_cloud_cover}, #{low_cloud_cover}, #{sunshine}
    )
  </insert>
  <!-- 全件取得クエリ -->
  <select id="selectAll" resultType="TWeatherForecastG">
    SELECT <include refid="allColumns"/>
    FROM <include refid="tableName"/>
  </select>
  <!-- 特定の配信における登録件数取得クエリ -->
  <select id="countDataInDistribution" resultType="Integer">
    SELECT COUNT(*)
    FROM <include refid="tableName"/>
    WHERE distribution_time = #{distributionTime}
  </select>
  <!-- 指定時間のデータ取得クエリ -->
  <select id="getDataWithForecastTime" resultType="TWeatherForecastG">
    SELECT <include refid="allColumns"/>
    FROM <include refid="tableName"/>
    WHERE
      location_id = #{locationId}
      <![CDATA[
        AND forecast_time = #{forecastTime}
      ]]>
    ORDER BY distribution_time DESC
    LIMIT 1
  </select>
</mapper>
